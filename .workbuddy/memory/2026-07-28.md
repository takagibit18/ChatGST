# 2026-07-28

## 熟悉 ChatGST 项目架构
- 项目 = 育儿补贴政策对话助手（README） + 本体智能体平台（docs/onto-platform-architecture.md 描述的 onto 这一半）。
- 两份文档：用户给的 `本体智能体平台.md`（onto-platform 概念：extract→derive→merge→review，冲突检测是硬规则非 LLM）；仓库内 `docs/onto-platform-architecture.md` 描述本项目如何迁移到多领域（onto-bridge 中间件 + pi-coding-agent 运行时）。
- 实际代码已验证：
  - `packages/ontology/src/step2-merge.ts`：merge 两阶段（dry_run 预览取 conflict_items → 确定性 resolutions：use_candidate/keep_existing/skip），无 LLM。冲突检测在远端平台侧。
  - `packages/pi-runtime-adapter/src/runtime.ts`：`PolicyAgentRuntime.answer()` 流水线 = 预算/并发闸 → 查询归一化 → (LLM)查询重写+BM25检索 → LLM rerank → buildEvidencePack → 校验/兜底 → agent loop 或 direct 生成 → schema+business 校验（可一次修复，否则安全模板）。
  - 5 个白名单工具在 `packages/tools`；模型 Test/DeepSeek 在 `packages/model-provider`；检索 BM25 在 `packages/policy-rag-adapter`。
  - onto 管道脚本：`scripts/run-step2-auto-modeling.ts` / `finalize-step2.ts` / `e2e-onto-test.ts`；本地用 `MOCK_ONTO=1`。
- 关键差距（本地 vs 线上）：规则提取/推导/引擎本地为 Mock（正则/if-else），线上靠 LLM+图引擎；Agent 运行时未对接 pi-coding-agent。
- 下一步用户可能想深入：运行时 answer 链路 / onto Step2 建模 / 规则引擎接入。
