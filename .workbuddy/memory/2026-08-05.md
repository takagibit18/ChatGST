# 2026-08-05 工作日志

## v2.1 Gold 人工验收批量回填
- 背景：用户已人工看完 v2.1 全部 gold，要求一次性标记为「人工通过」。
- 范围：`domains/childcare-subsidy/evals/v2.1/annotations/` 下 4 个 jsonl，共 143 条
  - retrieval.jsonl 80 / regression-v1.jsonl 13 / conversations.jsonl 20 / safety.jsonl 30
- 改动（逐行 JSON parse 后精确修改，保留 EOL 与字段顺序）：
  - `source_review_status`: `pending_review` → `human_approved`
  - `reviewer`: `null` → `"xinyuxing"`
  - 其余字段（notes/evidence/claims/required_facts 等）原样未动
- 备份：每个文件生成 `*.bak`（annotations 目录内），确认无误后可删除
- 验证：143 行 JSON 全部合法解析；字段级 diff 确认仅 2 字段变化；无 pending_review 残留
- 脚本：`C:\Users\xinyuxing\.workbuddy\binaries\node\workspace\mark-gold-approved.js`
- 下一步（未执行，待用户触发）：`pnpm eval:v2.1:prepare`（写入校验）+ `pnpm eval:v2.1:validate`（只读校验），方可解除 provisional gate

## notes 文案同步（同日追加）
- retrieval.jsonl 80 条 + regression-v1.jsonl 13 条的 `notes` 原为「…等待业务责任人复核。」，与 human_approved 状态矛盾
- 替换为「从K4原文显式quote和atomic claim构建，已通过人工复核。」
- 措辞用「人工复核」而非「业务责任人复核」：后者在 checklist 里指业务侧高风险口径签字，是另一层，不越界声称
- safety/conversations 无 notes 字段，未动；状态字段与 JSON 合法性复验通过

## 改 validate 脚本支持 human_approved + 跑校验（同日）
- prepare 首跑失败：`validate-eval-v2-1.ts` 第 85/115 行硬性要求 143 条全 `pending_review`，与 HUMAN-REVIEW-CHECKLIST.md 描述矛盾；但 schema 层（`evalReviewStatusSchema`）其实早已支持 human_approved
- 改 `scripts/validate-eval-v2-1.ts` 6 处：第 85/115 行放宽为"拒绝 rejected"；新增 reviewCounts 统计 + allHumanApproved；manifest.release_gate 动态（approved→human_review_passed）；manifest.review 动态 counts；console review_status 动态
- 环境坑：better-sqlite3 编译在 Node 24，managed node 22 报 ERR_DLOPEN_FAILED；改用 `PATH="/c/Program Files/nodejs:$PATH" pnpm eval:v2.1:prepare` 系统node24
- 结果：prepare + validate 均 valid:true；manifest release_gate=human_review_passed, review={pending:0,approved:143,rejected:0}；datasets 物化后 143 条全 human_approved
- 遗留：score-eval-v2-1.ts（humanReviewComplete:false / review_notice pending 硬编码）当时未改 → 后已改，见下

## 改 score + run 脚本联动 human review（同日）
- 用户决定改。改 `score-eval-v2-1.ts`：`humanReviewComplete` 读 manifest review counts（pending=0 && rejected=0 && approved>0）；`review_notice` 动态化；`quality_claim_allowed` 联动 `release_gate==="ready_for_release"`
- 改 `run-eval-v2-1.ts`：raw.release_gate 读 manifest（`datasetReleaseGate`，fallback blocked）
- 改 `tests/unit/eval-v2-1.test.ts` 2 处失败测试：断言从 pending_review/false 改为 human_approved + release_gate 联动一致
- 验证：score 跑通，**本地 Mock quality gate 恰好过了** + human review 过 → `release_gate=ready_for_release`, `quality_claim_allowed=true`, `review_notice="All Gold has passed human review."`；27 单元测试全过
- 注意：`ready_for_release`+`quality_claim_allowed=true` 是强声明，但本地 Mock runtime 下跑出，对外用需判断
- run 的 raw-predictions.json 未重跑（仍旧 release_gate=blocked），score 不读 raw.release_gate，报告已正确
